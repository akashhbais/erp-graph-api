services:
  - type: web
    name: erp-graph-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: DEBUG
        value: "false"
      - key: APP_DB_PATH
        value: /var/data/app.duckdb
    disk:
      name: erp_data
      mountPath: /var/data
      sizeGB: 1